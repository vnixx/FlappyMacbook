import SwiftUI
import SpriteKit

@main
struct FlappyMacBookApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 400, height: 600)
    }
}
